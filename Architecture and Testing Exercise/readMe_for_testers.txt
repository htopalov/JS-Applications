Each problem is supposed to be started stand alone and install dependencies before testing(npm install).
To start a test open problem with vs code and inside terminal type: cd test -> mocha 'the testFile'
